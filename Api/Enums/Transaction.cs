public enum TransactionType
{
    Income = 1,   // Ganhos
    Expense = 2,  // Gastos
    Reserve = 3   // Aporte na reserva
}

public enum TransactionSubType
{
    Common = 1,
    Future = 2,
    Constant = 3
}
